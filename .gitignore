# machine-specific - never commit
config.json
models.ini
# timestamped copies the app (and past rescues) leave beside them
config.json.backup-*
models.ini.backup-*
stats.json
wiki/
logs/
__pycache__/
*.pyc

# a user's cloned engine + build artifacts
llama.cpp/

# local editor / agent tooling config
.claude/
.codex/
.agents/
CLAUDE.md
AGENTS.md
venvLlamaForge/
.venv/
venv/

# agent design/plan artifacts (superpowers) - keep internal, don't publish
docs/superpowers/

# docs site build output + local-only working docs
site/
docs/content/_*.md
